<nav class="navbar navbar-expand-lg navbar-dark">
  <%= link_to "EduKat", root_path, class: "navbar-brand"  %>
  
    <div class="navbar-nav">
      <%= link_to "Home", root_path, class: "nav-link active"  %>
    </div>
    
    <div class="container">
      <div class="ml-auto mr-1">
          <% if user_signed_in?  %>
            <ul class="navbar-nav">
              <li class="nav-item dropdown">
                  <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  <%= current_user.first_name%> <%= current_user.last_name %>
                  </a>
                  <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                  <%= link_to "Edit profile", edit_user_registration_path, class: "dropdown-item"  %>
                  <% if current_user.role == "Mentor"  %>
                    <%= link_to "Add a course", new_course_path, class: "dropdown-item"  %>
                  <% end  %>
                  <%= link_to "View course list", courses_path, class: "dropdown-item"  %>
                  <%= link_to "Log Out", destroy_user_session_path, method: :delete,class: "dropdown-item"  %>
                  </div>
              </li>
          </ul>
          <% else  %>
            <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-outline-warning" %>
            <%= link_to "Log In", new_user_session_path, class: "btn btn-outline-success"  %>
          <% end  %>
      </div>
    </div>
    
</nav>